import { StyleSheet } from "react-native";

export const cardStyles = StyleSheet.create({
  container: {
    justifyContent: "center",
    margin: 10,

    padding: 5,
    borderWidth: 1,
  },
});
